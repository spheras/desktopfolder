#!/bin/sh

# TODO how could it be started with the normal user? (not root-sudo)
#if ( pidof org.spheras.desktopfolder );then
#    killall org.spheras.desktopfolder
#fi
#su $USER -c "/usr/bin/org.spheras.desktopfolder &"
#
echo "Remember: You'll need to logout/reboot to launch the application."

exit 0
